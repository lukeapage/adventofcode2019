(function() { const run = (program ) => {for(let i = 0; i < program.length; i+=4) { let opcode = program[i]; let a = program[program[i+1]], b = program[program[i+2]], pos = program[i+3]; if (opcode === 99) break; if (opcode === 1) { program[pos] = a+b; } else if (opcode === 2) {  program[pos] = a*b; } else { console.log('error'); } } return program;}; const tryit = (noun, verb) => { const program = document.body.innerText.split(',').map((n) => Number(n)); program[1] = noun; program[2] = verb; return run(program)[0];}; for(let x = 0; x <= 99; x++) { for(let y = 0; y <= 99; y++) { if(tryit(x, y) === 19690720) { console.log('hurray', x, y, 'so', 100 * x + y); } } }})()